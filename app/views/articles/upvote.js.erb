currentLocation = window.location.href
var upvote_span = document.querySelector(`#likes${'<%= @article.id %>'}`);
upvote_span.innerHTML = '<%= @article.votes_for.size %>';

if (currentLocation.includes('user')) {
  location.reload()
} else if (currentLocation.includes('questions')) {
  location.reload()
} else {
  console.log('I will not reload')
}
